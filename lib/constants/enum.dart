// ignore_for_file: constant_identifier_names

enum StorageKey {
  requestOTP,
  currentLanguage,
  allowMultipleSite,
  cookie,
  systemMode,
  appStartUp,
  usrId,
  tenantInfo,
  usePINCode,
  currentPOSProfile,
  lastUserLogin,
  currentCompany
}

enum ENV { serverUrl, apiAuth, aesIv, aesKey }

enum SystemMode { system, light, dark }
